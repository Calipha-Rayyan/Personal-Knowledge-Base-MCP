# --- Web framework ---
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
pydantic>=2.6.0
pydantic-settings>=2.2.0
slowapi>=0.1.9

# --- Database / Auth ---
sqlalchemy>=2.0.0
alembic>=1.13.0
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
bcrypt<4.1
python-multipart>=0.0.9
email-validator>=2.1.0

# --- Document ingestion ---
pypdf>=3.0.0
python-docx>=0.8.11
python-pptx>=0.6.21
markdown>=3.4.0

# --- Embeddings / Vector search ---
# All three of these are pinned TOGETHER as a known-compatible set.
# Pinning sentence-transformers alone is not enough: it depends on
# transformers + tokenizers, and pip is free to install newer versions
# of those that are incompatible with each other even when
# sentence-transformers itself is pinned. This exact combination is
# tested and known to work.
sentence-transformers==3.0.1
transformers==4.41.2
tokenizers==0.19.1
huggingface-hub==0.23.4
qdrant-client>=1.7.0

# --- MCP ---
fastmcp>=0.2.0